# Production environment settings

# API Base URL for production
# In production, this should be empty to use relative URLs
# This assumes the frontend and backend are served from the same domain
VITE_API_BASE_URL=https://bfstats.io

# Discord OAuth Configuration
# Get the Client ID from https://discord.com/developers/applications
# Note: Client Secret should be configured on the backend, not here
# Make sure to add the production redirect URI: https://1942.munyard.dev/auth/discord/callback
VITE_DISCORD_CLIENT_ID=1410567423119196251

# Azure Application Insights Configuration
# Get the connection string from your Azure Application Insights resource
# Format: InstrumentationKey=xxx;IngestionEndpoint=https://xxx.in.applicationinsights.azure.com/;LiveEndpoint=https://xxx.livedata.monitor.azure.com/
# This should match the APPLICATIONINSIGHTS_CONNECTION_STRING used in the API
VITE_APPLICATIONINSIGHTS_CONNECTION_STRING=